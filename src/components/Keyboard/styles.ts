import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    rowsContainer: {
        alignItems: "center",
        margin: 10
    },
    row: {
        flexDirection: "row"
    },
    buttonContainer: {
        justifyContent: "center",
        alignItems: "center",
        height: 100
    }
});